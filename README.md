# CoinMarketCapApiClient

Just some fun with the CoinMarketCap API.

## Runtime settings

The appsettings.json file required at runtime needs to be added with the following content...
```
{
  "CoinMarketCap": {
    "Environment": "pro",
    "ApiKey": "REPLACE_WITH_YOUR_API_KEY"
  }
}
```
